import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <nav className='navbar'>
            <h1>First Choice Car Palase</h1>
        </nav>
    );
};

export default Header;